name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
      - develop
    paths:
      - 'website/**'
      - '.github/workflows/deploy.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: ${{ github.ref == 'refs/heads/main' && 'prod' || 'dev' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure deployment
        run: |
          if [[ ${{ github.ref }} == 'refs/heads/main' ]]; then
            # For main branch - deploy to root
            echo 'baseurl: "/venue-playlists"' > website/_config.yml
          else
            # For develop branch
            # Create a temporary directory
            mkdir -p /tmp/website-content
            # Copy all website content to temp
            cp -r website/* /tmp/website-content/
            # Clear website directory
            rm -rf website/*
            # Create dev directory
            mkdir -p website/dev
            # Copy content to dev directory
            cp -r /tmp/website-content/* website/dev/
            # Set dev config
            echo 'baseurl: "/venue-playlists/dev"' > website/dev/_config.yml
          fi

      - name: Setup Pages
        uses: actions/configure-pages@v4
          
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'website'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
