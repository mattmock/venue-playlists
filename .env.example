### Application Settings ###
# Environment (development/production)
FLASK_APP=venue_playlists_api
FLASK_ENV=development
FLASK_DEBUG=1

### Server Configuration ###
# Production only - generate using: python -c 'import secrets; print(secrets.token_hex())'
# SECRET_KEY=<generated-secret-key>
# ALLOWED_ORIGINS=https://your-domain.com

### Spotify API Configuration ###
# Get these from Spotify Developer Dashboard: https://developer.spotify.com/dashboard
SPOTIFY_CLIENT_ID=<your-spotify-client-id>
SPOTIFY_CLIENT_SECRET=<your-spotify-client-secret>
# Don't set this manually - it will be auto-populated during setup
SPOTIFY_REFRESH_TOKEN=
# Must match the redirect URI in your Spotify App settings
SPOTIPY_REDIRECT_URI=http://localhost:8888/callback

### Paths and Directories ###
# Optional - defaults will be used if not set
# VENUE_DATA_DIR=/path/to/data
# LOGS_DIR=/path/to/logs

### Development Tools ###
# Optional - for local development only
PYTHONPATH=.
LOG_LEVEL=INFO
SAVE_ALL_SCREENSHOTS=false

### OpenAI Configuration ###
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=<your-openai-api-key>

### CORS Settings ###
# Comma-separated list of allowed origins
# Development default (don't use in production):
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000

# Production example (uncomment and modify):
# ALLOWED_ORIGINS=https://venue-playlists.vercel.app,https://*.venue-playlists.vercel.app 